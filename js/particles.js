AILab.Particles=class{constructor(){this.list=[];this.max=180}burst(x,y,color="#51e9ff",n=12,s=150){for(let i=0;i<n&&this.list.length<this.max;i++){const a=Math.random()*Math.PI*2,v=Math.random()*s;this.list.push({x,y,vx:Math.cos(a)*v,vy:Math.sin(a)*v,life:.3+Math.random()*.5,max:.8,color,size:1+Math.random()*3})}}trail(x,y){if(this.list.length<this.max)this.list.push({x,y,vx:0,vy:0,life:.25,max:.25,color:"#58ecff",size:10})}update(dt){for(const p of this.list){p.life-=dt;p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=.97;p.vy*=.97}this.list=this.list.filter(p=>p.life>0)}draw(ctx,camera){for(const p of this.list){ctx.globalAlpha=p.life/p.max;ctx.fillStyle=p.color;ctx.fillRect(p.x-camera.x-p.size/2,p.y-camera.y-p.size/2,p.size,p.size)}ctx.globalAlpha=1}};
