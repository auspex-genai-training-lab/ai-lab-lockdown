AILab.Player=class{constructor(){this.x=4*64;this.y=5*64;this.r=16;this.angle=0;this.hp=100;this.stamina=100;this.battery=100;this.flashlight=true;this.card=0;this.inventory=[];this.invuln=0;this.walk=0}update(dt,input,map,particles,stats){let x=(input.ArrowRight||input.KeyD?1:0)-(input.ArrowLeft||input.KeyA?1:0),y=(input.ArrowDown||input.KeyS?1:0)-(input.ArrowUp||input.KeyW?1:0),len=Math.hypot(x,y);if(len){x/=len;y/=len;this.angle=Math.atan2(y,x);this.walk+=dt*10}const dash=(input.ShiftLeft||input.ShiftRight)&&this.stamina>2&&len;const speed=dash?340:190;if(dash){this.stamina-=35*dt;stats.dashes+=dt;particles.trail(this.x,this.y)}else this.stamina=Math.min(100,this.stamina+22*dt);AILab.Collision.move(this,x*speed*dt,y*speed*dt,map.solidRects());if(this.flashlight)this.battery=Math.max(0,this.battery-dt*.45);this.invuln=Math.max(0,this.invuln-dt)}hurt(from,sound,particles){if(this.invuln)return;this.hp-=20;this.invuln=1;const a=Math.atan2(this.y-from.y,this.x-from.x);this.x+=Math.cos(a)*35;this.y+=Math.sin(a)*35;sound.hurt();particles.burst(this.x,this.y,"#ff3b4f",18,180)}draw(ctx,c){ctx.save();ctx.translate(this.x-c.x,this.y-c.y);ctx.rotate(this.angle);ctx.globalAlpha=this.invuln&&Math.floor(this.invuln*12)%2?.35:1;ctx.fillStyle="#0008";ctx.beginPath();ctx.ellipse(0,12,18,9,0,0,Math.PI*2);ctx.fill();ctx.fillStyle="#d9faff";ctx.fillRect(-9,-12,19,28);ctx.fillStyle="#51e9ff";ctx.fillRect(5,-8,18,8);ctx.fillStyle="#132b3c";ctx.beginPath();ctx.arc(0,-14,10,0,Math.PI*2);ctx.fill();ctx.restore()}};
